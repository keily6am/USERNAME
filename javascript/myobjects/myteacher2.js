const arr1 = [1,2,3,4,5];

console.log(arr1);

// remove the last element/object of the array
//arr1.pop();

// remove the first element of the array
//arr1.shift()

//remove something in between
// .splice (location how many)
//arr1.splice(2,2);

// add something to front of the list
arr1.unshift(6);

console.log(arr1);
